# Scripts

This folder is reserved for future root-level helper scripts, maintenance tasks, data utilities, and local automation.

This pass keeps the working launchers in `QUICK_LAUNCH/` and avoids adding behavior-changing automation.
